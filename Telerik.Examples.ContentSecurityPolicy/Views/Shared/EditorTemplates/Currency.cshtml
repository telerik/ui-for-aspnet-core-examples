@model decimal?

@(Html.Kendo().Template().AddComponent(x => x.CurrencyTextBoxFor(m => m)
      .HtmlAttributes(new { style = "width:100%", title = Html.ViewContext.ViewData.TemplateInfo.GetFullHtmlFieldName("") })
      .Min(0)
    )
)

